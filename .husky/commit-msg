#!/usr/bin/env sh

# Validate commit message format (conventional commits, requires package.json with commitlint)
if command -v npx >/dev/null 2>&1 && [ -f "package.json" ]; then
  npx --no -- commitlint --edit "$1"
fi
